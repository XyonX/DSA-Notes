int maximumSubarraySumSizeK(vector<int>arr, int k ){
    int sum=0;
    int maxSum = INT_MIN;
    int l=0;
    for(int r=0;r<arr.size();r++){
        sum+=arr[r];
        if((r-l)+1>k){
            sum-=arr[l];
            l++;
        }
        maxSum=max(sum,maxSum);
    }
    return maxSum;
}